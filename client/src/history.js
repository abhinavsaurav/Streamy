// we are importing the package 'history' installed along with createBrowserHistory
// after which we just need to run it by exporting it
import { createBrowserHistory } from "history";

export default createBrowserHistory();
